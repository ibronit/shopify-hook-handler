{
  "name": "shopify-hook-handler",
  "version": "1.0.0",
  "description": "Nodejs API to handle shopify hooks",
  "main": "index.js",
  "scripts": {
    "db:migrate": "node ./script/migrate",
    "db:migrate-undo": "node ./script/migrateUndo",
    "pretest": "NODE_ENV=test npm run db:migrate",
    "test": "NODE_PATH=./ NODE_ENV=test mocha --timeout=10000",
    "posttest": "NODE_ENV=test npm run db:migrate-undo",
    "dev": "nodemon --inspect",
    "start": "node index.js"
  },
  "author": "ibronit",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "mocha": "^5.2.0",
    "mocha-cli": "^1.0.1",
    "nodemon": "^1.17.5"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "child-process-promise": "^2.2.1",
    "express": "^4.16.3",
    "mysql2": "^1.5.3",
    "sequelize": "^4.38.0",
    "sequelize-cli": "^4.0.0"
  }
}
